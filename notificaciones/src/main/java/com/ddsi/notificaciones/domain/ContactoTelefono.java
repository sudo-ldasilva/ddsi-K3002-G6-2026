package com.ddsi.notificaciones.domain;

public class ContactoTelefono implements Contacto {
    @Override
    public void enviarMensaje(String mensaje, String direccion) {
       // System.out.println("Enviando SMS a " + codigoDeArea + " " + numero + ": " + mensaje)
    }
}

