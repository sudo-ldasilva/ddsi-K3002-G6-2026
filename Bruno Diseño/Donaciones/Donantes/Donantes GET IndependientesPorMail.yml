info:
  name: Donantes GET IndependientesPorMail
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8080/donantes/mail@empresa.com/independientesPorMail
  body:
    type: json
    data: |-
      {
        "direccion": "asuntoLicitos@empresa.com",
        "tipoContacto": "mail"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
